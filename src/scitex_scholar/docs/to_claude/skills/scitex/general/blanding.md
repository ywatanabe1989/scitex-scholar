# SciTeX Branding

Logo text: SciT**e**X — the "e" is tilted -21deg (inspired by Google's inclined "e").

```css
.brand-e {
  display: inline-block;
  transform: rotate(-21deg);
  transform-origin: 50% 70%;
  color: inherit;
}
```

```html
SciT<span class="brand-e">e</span>X
```
